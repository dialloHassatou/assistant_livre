<h1>Editing personne</h1>

<%= render 'form' %>

<p>Projets :</p><br />
  <%= collection_select(:personne, :scene_ids, @scenes, :id, :recit, {:prompt => " "}, { :multiple => true })
  <p>

<%= link_to 'Show', @personne %> |
<%= link_to 'Back', personnes_path %>
