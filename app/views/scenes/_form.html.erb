<%= form_for(@scene) do |f| %>
  <% if @scene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scene.errors.count, "error") %> prohibited this scene from being saved:</h2>

      <ul>
      <% @scene.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recit %><br>
    <%= f.text_area :recit %>
  </div>
  <div class="field">
    <%= f.label :lieu %><br>
    <%= f.text_field :lieu %>
  </div>
  <div class="field">
    <%= f.label :debut %><br>
    <%= f.date_select :debut %>
  </div>
  <div class="field">
    <%= f.label :fin %><br>
    <%= f.date_select :fin %>
  </div>
 <div class='field'>
         <%= f.label :Chapitre %><br>
        <%= collection_select(:anecdote, :chapitre_id, Chapitre.all, :numero, :numero) %>
      </div>

 <div class="field">
 <% Personne.all.each do |personne| %>
 <label><%= personne.name %>
   <%= check_box_tag "scene[personne_ids][]"%> 
 </label>
 <% end %>
</div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
